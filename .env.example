# Example environment variables for local development and documentation.
# Do NOT put secrets in source control. Copy this to `.env` and fill values.

# Server
PORT=3000
NODE_ENV=development

# Application
APP_HOST=localhost
APP_PROTOCOL=http

# Database (example)
## The code prefers `OD_DB_URL` if present. `DATABASE_URL` is still accepted as a fallback.
OD_DB_URL=postgres://user:password@localhost:5432/odin_db
DATABASE_URL=postgres://user:password@localhost:5432/odin_db

# Session / secrets
SESSION_SECRET=replace-with-a-secure-random-value
## Server-side secrets (preferred keys used by the app)
OD_COOKIE_SECRET=replace-with-a-secure-random-value
OD_PKCE_ID=example-client-id
OD_PKCE_SECRET=replace-with-a-secure-random-value
OD_OAUTH_URL=https://oidc.example/.well-known/openid-configuration

# Third-party integrations (optional)
SENTRY_DSN=
FEATURE_FLAG_EXAMPLE=false

#+ Optional runtime / server settings (override defaults)
#+ OD_PORT and OD_HOST are read by the server; PORT is still supported as a fallback.
OD_PORT=3000
OD_HOST=0.0.0.0

#+ TLS file paths (when running in secure mode)
OD_TLS_KEY=/etc/tls/tls.key
OD_TLS_CERT=/etc/tls/tls.crt

#+ Rate limiting
OD_RATE_LIMIT_MAX=100

#+ Telemetry: OTLP HTTP exporter endpoint (optional)
#+ When set, OpenTelemetry will export traces to this endpoint.
OTEL_EXPORTER_OTLP_ENDPOINT=

#+ CI / dev helpers
RUN_MIGRATIONS_SEED=0
