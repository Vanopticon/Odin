# Example environment variables for local development and documentation.
# Do NOT put secrets in source control. Copy this to `.env` and fill values.

# Server
PORT=3000
NODE_ENV=development

# Application
APP_HOST=localhost
APP_PROTOCOL=http

# Database (example)
DATABASE_URL=postgres://user:password@localhost:5432/odin_db

# Session / secrets
SESSION_SECRET=replace-with-a-secure-random-value

# Third-party integrations (optional)
SENTRY_DSN=
FEATURE_FLAG_EXAMPLE=false
